defaults:
  # Compose eval config from the same model/experiment groups.
  - model: transformer
  - experiment: common
  - paths: eval
  - _self_

experiment:
  # Keep eval seed separate for stable, repeatable test masking.
  seed: 123

hydra:
  run:
    dir: .
  output_subdir: null
